* Create an action package - three actions, one reaction (keep track on actions used)
* Create an inspiration package - toggle inspiration


* Only load package if selected.
OR
* Only load when swithing to path `/actions` in web-app

